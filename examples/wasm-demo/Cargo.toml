[package]
name = "wasm-demo"
version = "0.1.0"
edition = "2021"
description = "Browser demo for reed-solomon-simd via WebAssembly"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
reed-solomon-simd = { path = "../.." }
wasm-bindgen = "0.2"
js-sys = "0.3"
